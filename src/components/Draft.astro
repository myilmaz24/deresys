---
// COPY.md'de [DOLDURULACAK] işaretli bölümler için.
// İçerik uydurulmaz; hangi bilginin gerektiği olduğu gibi gösterilir.
const { need, label = 'DOLDURULACAK' } = Astro.props;
---
<div class="draft">
  <span class="draft__label">{label}</span>
  <p class="draft__need">{need}</p>
</div>
